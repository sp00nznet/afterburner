# afterburner - ReXGlue Recompiled Project
#
# SDK boilerplate lives in generated/rexglue.cmake.

cmake_minimum_required(VERSION 3.25)
project(afterburner LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

include(generated/rexglue.cmake)

# Sources
set(AFTERBURNER_SOURCES
    src/main.cpp
)

if(WIN32)
    add_executable(afterburner WIN32 ${AFTERBURNER_SOURCES})
else()
    add_executable(afterburner ${AFTERBURNER_SOURCES})
endif()

rexglue_setup_target(afterburner)
